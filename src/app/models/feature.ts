export class Feature {

  featureText: string;
  featureIconText: string;

  constructor(featureText: string, featureIconText: string) {
    this.featureText = featureText;
    this.featureIconText = featureIconText;
  }
}
