export class Benefit {

  private title: string;
  private description: string;
  private iconText: string;

  constructor(title: string, description: string, iconText: string) {
    this.title = title;
    this.description = description;
    this.iconText = iconText;
  }
}
